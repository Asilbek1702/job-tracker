# 📊 Job Tracker - Система управления поиском работы

![Python](https://img.shields.io/badge/Python-3.11+-blue.svg)
![FastAPI](https://img.shields.io/badge/FastAPI-0.104+-green.svg)
![License](https://img.shields.io/badge/License-MIT-yellow.svg)

Полнофункциональное веб-приложение для отслеживания откликов на вакансии с REST API на FastAPI и адаптивным интерфейсом для соискателей и работодателей.

## 🎯 О проекте

Job Tracker - это демонстрационный проект для портфолио Junior Python Developer, который показывает навыки работы с:
- Backend разработкой (FastAPI, SQLite/PostgreSQL)
- REST API и аутентификацией (JWT)
- Frontend разработкой (HTML, CSS, JavaScript)
- Базами данных и ORM (SQLAlchemy)
- Git и GitHub

## ✨ Основные возможности

### 👤 Для соискателей (ищу работу):
- **📄 Резюме** - Создание и редактирование резюме
- **📨 Мои отклики** - Управление откликами на вакансии
- **💼 Все вакансии** - Просмотр доступных вакансий
- **📊 История откликов** - Хронология всех откликов с изменениями статусов

### 💼 Для работодателей:
- **👥 Управление кандидатами** - Добавление и отслеживание кандидатов
- **📈 Аналитика** - Статистика по кандидатам
- **🔍 Фильтрация** - Поиск по имени и статусу

### 🔐 Общие возможности:
- ✅ Регистрация и аутентификация (JWT)
- ✅ CRUD операции для вакансий/кандидатов
- ✅ Статусы отслеживания (Applied, Interview, Offer, Rejected)
- ✅ Аналитика и статистика
- ✅ Фильтрация и поиск
- ✅ Адаптивный дизайн
- ✅ Swagger документация API

## 🛠 Технологический стек

### Backend:
- **Python 3.11+**
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **SQLite** - база данных (легко переключается на PostgreSQL)
- **JWT** - аутентификация и авторизация
- **Pydantic** - валидация данных
- **Bcrypt** - хеширование паролей

### Frontend:
- **HTML5** - семантическая разметка
- **CSS3** - современные стили (Flexbox, Grid, Animations)
- **Vanilla JavaScript** - чистый JS без фреймворков
- **Fetch API** - работа с REST API

### Инструменты:
- **Git** - система контроля версий
- **GitHub** - хостинг кода
- **Uvicorn** - ASGI сервер

## 📁 Структура проекта

```
job_tracker/
├── backend/
│   ├── main.py              # Основной файл FastAPI приложения
│   ├── requirements.txt     # Зависимости Python
│   └── job_tracker.db      # SQLite база данных (создаётся автоматически)
├── frontend/
│   ├── index.html          # HTML структура
│   ├── styles.css          # Стили приложения
│   └── app.js              # JavaScript логика
├── README.md               # Документация проекта
└── .gitignore             # Игнорируемые файлы для Git
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)
- Git

### Установка

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/ваш-username/job-tracker.git
cd job-tracker
```

2. **Создайте виртуальное окружение**
```bash
# Windows
python -m venv venv
venv\Scripts\activate

# Linux/Mac
python3 -m venv venv
source venv/bin/activate
```

3. **Установите зависимости**
```bash
cd backend
pip install -r requirements.txt
```

4. **Запустите backend сервер**
```bash
uvicorn main:app --reload
```

Сервер запустится на `http://127.0.0.1:8000`

5. **Запустите frontend (в новом терминале)**
```bash
cd frontend
python -m http.server 8080
```

Frontend будет доступен на `http://localhost:8080`

### Первый запуск

1. Откройте браузер и перейдите на `http://localhost:8080`
2. Зарегистрируйте новый аккаунт
3. Выберите роль: **Ищу работу** или **Работодатель**
4. Начните использовать приложение!

## 📖 API Документация

После запуска сервера документация доступна по адресам:
- **Swagger UI**: `http://127.0.0.1:8000/docs`
- **ReDoc**: `http://127.0.0.1:8000/redoc`

### Основные эндпоинты

#### Аутентификация

```http
POST /auth/register
POST /auth/login
GET  /auth/me
```

#### Вакансии / Кандидаты

```http
GET    /jobs           # Получить список
POST   /jobs           # Создать новую запись
GET    /jobs/{id}      # Получить по ID
PUT    /jobs/{id}      # Обновить
DELETE /jobs/{id}      # Удалить
```

#### Аналитика

```http
GET /analytics/summary  # Получить статистику
```

### Пример использования API

**Регистрация:**
```bash
curl -X POST http://127.0.0.1:8000/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "password": "password123",
    "user_type": "job_seeker"
  }'
```

**Получение вакансий:**
```bash
curl -X GET http://127.0.0.1:8000/jobs \
  -H "Authorization: Bearer YOUR_TOKEN"
```

## 🎨 Интерфейс

### Скриншоты

#### Соискатель
- **Резюме**: Форма создания и редактирования резюме
- **Мои отклики**: Список откликов с фильтрацией
- **История**: Временная шкала всех откликов

#### Работодатель
- **Кандидаты**: Управление кандидатами на вакансии
- **Аналитика**: Статистика по кандидатам

### Статусы

| Статус | Соискатель | Работодатель |
|--------|-----------|--------------|
| Applied | Отправлен отклик | Новый кандидат |
| Interview | Назначено интервью | Назначено интервью |
| Offer | Получен оффер | Отправлен оффер |
| Rejected | Отказ | Отказ |

## 🗄 Модель данных

### User (Пользователь)
```python
{
  "id": int,
  "email": str,
  "hashed_password": str,
  "user_type": "job_seeker" | "employer",
  "created_at": datetime
}
```

### Job (Вакансия/Кандидат)
```python
{
  "id": int,
  "user_id": int,
  "company_name": str,      # Для соискателя: компания, для работодателя: имя кандидата
  "position": str,          # Для соискателя: должность, для работодателя: вакансия
  "status": str,
  "salary": str | null,
  "link": str | null,
  "notes": str | null,
  "created_at": datetime,
  "updated_at": datetime
}
```

## 🔒 Безопасность

⚠️ **ВАЖНО**: Перед деплоем в продакшн:

1. Измените `SECRET_KEY` в `main.py`:
```python
SECRET_KEY = os.getenv("SECRET_KEY", "your-super-secret-key-min-32-characters")
```

2. Используйте переменные окружения для конфиденциальных данных

3. Переключитесь на PostgreSQL для продакшн

4. Включите HTTPS

5. Настройте CORS правильно

## 🚢 Деплой

### Heroku

```bash
# Создайте Procfile
echo "web: uvicorn backend.main:app --host=0.0.0.0 --port=${PORT:-5000}" > Procfile

# Деплой
heroku create your-app-name
git push heroku main
```

### Docker

```dockerfile
# Dockerfile (пример)
FROM python:3.11-slim
WORKDIR /app
COPY backend/requirements.txt .
RUN pip install -r requirements.txt
COPY backend/ .
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

## 🧪 Тестирование

```bash
# Установите pytest
pip install pytest pytest-asyncio httpx

# Запустите тесты
pytest
```

## 📝 TODO / Roadmap

- [ ] Добавить тесты (pytest)
- [ ] Миграции с Alembic
- [ ] Экспорт данных в CSV/Excel
- [ ] Email уведомления
- [ ] Календарь интервью
- [ ] Теги и категории вакансий
- [ ] Прикрепление файлов резюме
- [ ] Поиск вакансий от реальных API (hh.ru, LinkedIn)
- [ ] Docker контейнеризация
- [ ] CI/CD pipeline
- [ ] Frontend на React

## 🤝 Вклад в проект

Вклад приветствуется! Чтобы внести изменения:

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👨‍💻 Автор

**Ваше Имя**
- GitHub: [@ваш-username](https://github.com/Asilbek1702)
- Email: asilbektashpulatov687@gmail.com

## 🙏 Благодарности

- [FastAPI](https://fastapi.tiangolo.com/) - за отличный фреймворк
- [SQLAlchemy](https://www.sqlalchemy.org/) - за мощный ORM
- Сообществу Python за поддержку

---

⭐ Если проект понравился, поставьте звезду на GitHub!

**Создано как портфолио проект для демонстрации навыков Junior Python Developer**